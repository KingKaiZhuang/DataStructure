# CPE 題目練習追蹤看板 (CPE Practice Tracker)

這是一個為程式能力檢定 (CPE, Collegiate Programming Examination) 考生與程式學習者設計的**現代化、高效能互動式練習進度追蹤看板**。本專案將原本靜態的 Markdown 題庫表格轉換成具備狀態管理、批次修改、進度統計與美學設計的單頁 Web 應用程式 (SPA)。

* **線上體驗地址**: [https://cpe-dashboard.vercel.app](https://cpe-dashboard.vercel.app)

---

## ✨ 核心功能特點

1. **📊 視覺化進度儀表板 (Stats Panel)**
   * 自動計算「已解決」、「學習中」與「未開始」的題數比例。
   * 搭載動態漸層進度條與平台分佈統計 (ZeroJudge vs UVa)。
   * 提供「一鍵重置」功能以清除所有練習進度。

2. **⚡️ 批次修改解題狀態 (Batch Status Update) - *全新功能！***
   * **多選模式**: 無論在表格視圖或卡片視圖，都可以透過複選框選擇多個題目。
   * **智慧全選**: 支援「一鍵全選目前頁面所有篩選結果」，只作用於您當前搜尋與篩選出來的題目。
   * **浮動操作列**: 當選取任一題目時，底部會自動彈出半透明磨砂玻璃質感的控制列，支援批次標記為「已解決」、「學習中」或「未開始」，並提供快速取消選擇。

3. **🔍 強大精準的搜尋與篩選 (Search & Filters)**
   * **即時搜尋**: 支援以題目名稱、UVA 題號、ZeroJudge ID 甚至流水號進行即時模糊比對。
   * **平台篩選**: 快速分類 ZeroJudge 或 UVa / VJudge 的題目。
   * **狀態篩選**: 快速找出哪些是已經解決、尚未開始或正在學習的題目。
   * **靈活排序**: 提供題號升降序、題目名稱字母順序等排序規則。
   * **智慧連動**: 切換搜尋或篩選條件時，會自動清除已選取的項目，防止意外修改到不可見題目的狀態。

4. **🔄 狀態本地持久化 (Persistence)**
   * 所有題目的練習進度狀態均即時同步至瀏覽器的 `localStorage` 中。
   * 重新整理網頁、關閉瀏覽器甚至離線使用，進度資料皆不會遺失。

5. **📱 響應式雙版面模式 (Dual View Mode)**
   * **表格視圖 (Table)**: 緊湊直觀，完美整合平台練習連結與 PDF 原題下載。
   * **卡片視圖 (Grid)**: 卡片式卡柵，大字體呈現，並提供滑鼠懸停發光 (Glow Mode) 以及直覺的狀態點選按鈕。
   * **行動裝置友善**: 介面會自動適應手機、平板與桌上型電腦的螢幕寬度。

6. **🔮 磨砂玻璃美學 (Glassmorphism Dark Mode)**
   * 預設為精緻的暗色調 (Dark Mode)，支援切換至亮色模式。
   * 使用流線型的漸層動態背景光球 (Animated BG Blobs)，搭配邊框發光與流暢的轉場微動畫，帶來極致的視覺美感。

---

## 🛠️ 技術棧 (Tech Stack)

* **前端核心**: React 18 / TypeScript
* **建構工具**: Vite 8 (提供極速的開發編譯與輕量化打包)
* **樣式系統**: Vanilla CSS (完全自定義，無第三方 UI 框架負擔，實現 100% 自由度的視覺美學)
* **部署平台**: Vercel (支援持續整合與邊緣運算靜態託管)

---

## 🚀 快速啟動與開發 (Quick Start)

### 1. 安裝環境與依賴
請確保您的系統已安裝 Node.js (建議 v18+)。在 `cpe-dashboard` 目錄下執行：
```bash
npm install
```

### 2. 啟動開發伺服器
執行以下指令開啟本地開發環境，預設會在 `http://localhost:5173` 運行：
```bash
npm run dev
```

### 3. 編譯生產版本
將程式碼編譯並打包為高度優化的靜態資源檔案 (輸出至 `dist/` 資料夾)：
```bash
npm run build
```

---

## 📦 Vercel 部署指南 (Deployment)

專案已配置 `vercel.json` 檔案以支援 Single Page Application (SPA) 的路由轉發。

若要手動部署或推播更新：
```bash
# 安裝或調用 Vercel CLI 進行部署
npx vercel --prod
```
Vercel 將會讀取專案設定，自動呼叫 `npm run build` 並將 `dist/` 目錄部署到雲端。
